name: schedule

on:
  push:
    branches:
      - dev
  schedule:
    - cron: '09 17 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "atualiza às 18:09h"