name: Schedule wk
on:
  push:
    branches: [main]
  schedule:
    - cron: 55 16 * * *
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow automático - 16:55h"