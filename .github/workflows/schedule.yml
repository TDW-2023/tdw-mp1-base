name: schedule

on:
  push:
    branches:
      - main
  schedule:
    - cron: '16 17 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Execute command
        run: echo "atualiza às 18:16h"